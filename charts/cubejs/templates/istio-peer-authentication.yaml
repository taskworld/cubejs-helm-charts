{{- if .Values.istio.enabled }}
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: cube-mtls-strict
  namespace: {{ .Values.namespace }}
spec:
  mtls:
    mode: STRICT
{{- end }}
